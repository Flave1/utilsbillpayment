@model PagingResult<AgentRevenueListingModel>


@if (Model.List != null && Model.List.Count() > 0)
{
    foreach (var item in Model.List)
    {
        <tr class="odd gradeX">
            <td style="text-align:right"> @item.CreatedAt</td>
            <td style="text-align:right"> @item.PosNumber</td>
            <td style="text-align:left">@item.VendorName</td>
            <td> @item.Type</td>
            <td style="text-align:center;"> @item.TransactionId</td>
            <td style="text-align:right"> @item.ChkNoOrSlipId</td>
            <td style="text-align:right"><strong><a href="#">@(item.Amount == null ? "0" : string.Format("{0:N0}", item.Amount)) </a></strong></td>
            <td style="text-align:right"><strong>@(item.VendorPercentageAmount == null ? "0" : string.Format("{0:N0}", item.VendorPercentageAmount))</strong></td>
            <td style="text-align:right"><strong>@(item.AgentPercentageAmount == null ? "0" : string.Format("{0:N0}", item.AgentPercentageAmount))</strong></td>

        </tr>
    }
}
else
{
    <tr class="odd gradeX">
        <td colspan="10" style="font-weight:bold;">SEARCH COMPLETE! No marching record found</td>
    </tr>
}


